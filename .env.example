# Resend (https://resend.com) – get your API key from the dashboard.
# Required for the contact form to send messages to your email.
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional: email where you receive contact form messages (default: kiransai2312@gmail.com)
# CONTACT_EMAIL=kiransai2312@gmail.com

# Optional: "From" address (Resend free tier uses onboarding@resend.dev until you verify a domain)
# RESEND_FROM_EMAIL=Portfolio <onboarding@resend.dev>

# Notes for Vercel deployment:
# - Add the same key in your Vercel project under Settings → Environment Variables as `RESEND_API_KEY`.
# - Do NOT use NEXT_PUBLIC_ prefix; keep this server-only.
# - You can add it via the Vercel CLI: `vercel env add RESEND_API_KEY production`.
